language: node_js
sudo: required
node_js:
  - "0.12"
  - "0.11"
  - "0.10"

install:
 - sudo npm install -g istanbul

# run tests + coverage
script:
  - ./test.sh

# send coverage report to coveralls.io
after_success:
  - coveralls

# email notifications
notifications:
    email: false